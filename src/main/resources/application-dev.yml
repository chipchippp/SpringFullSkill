server:
  port: 8080
spring:
  config:
    activate:
      on-profile: dev
open:
  api:
    title: Sample Code
    version: v1.0.0
    description: Sample Code
    serverUrl: http://localhost:${server.port}
    serverName: server test